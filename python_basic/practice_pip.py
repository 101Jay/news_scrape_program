#파이썬에 이미 잘 개발해놓은 패키지들을 활용하자!
#구글 -> pypi : 패키지들 모아놓은 사이트.
#검색 후 -> pip install ~~~ 로 되어있는 것 복사 후 터미널 창에 입력
#예제 코드를 이용해 감을 잡아본다.

#pip list -> 현재 설치되어있는 패키지 리스트 확인
#pip show 패키지 이름 -> 패키지 정보 알려줌
#pip install --upgrade 패키지 이름 -> 패키지 업그레이드
#pip uninstall 패키지 이름 ->  패키지 삭제

# from bs4 import BeautifulSoup
# soup = BeautifulSoup("<p>Some<b>bad<i>HTML")
# print(soup.prettify())